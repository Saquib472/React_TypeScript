# TypeScript With React

## Props with Type 
- Check The Greet.tsx file component for reference of passing props type of `String, Number and Boolean`.
- Check The Person.tsx file component for refernce of passing props type of `Object`.
- Check The PersonList.tsx component for reference of passing props type `Array of Objects`.
- Check The Status.tsx component for reference of passing props type `Union`.
- Check The Heading.tsx component for reference of passing props type `Children`.
- Check The Oscar.tsx component for reference of passing props type `React Component`.
- Check The Greet.tsx component for reference of passing `Optional props`.
- Check The Button.tsx component for reference of passing props type `Event On Button Click`.
- Check The Input.tsx component for reference of passing props type `Event On Input Change`.
- Check The Container.tsx component for reference of passing props type `Style`.
- Can `Destructure Props` , Check The Input.tsx for reference.
- Can `Export and Import Types` , Check `The Person.tsx and PersonName.types.tsx and PersonList.tsx` for refernce.

## UseState with Type
- Check The LoggedIn.tsx And User.tsx Component for reference of type used in `useState Hook`

## UseReducer with Type
- Check The Counter.tsx for reference.

## Context API with Type
- Check The ThemeContext.tsx And ThemeContextProvider.tsx And Person.tsx
- For the future value setting the Type Chack UserContext.tsx And UserContextProvider.tsx And User.tsx.

## useRef hook with Type
- useRef Hook is used for referencing the Dom Element Directly And initializing a value to a variable which can be mutate and not re-render while change and remember the value.
- Type check with useRef. Check the DomRef.tsx And MutateRef.tsx

## Typing Class Component